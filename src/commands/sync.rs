//! Main sync command

use crate::Config;

/// Run the sync operation
pub fn run(_config: Config) -> Result<(), crate::types::KopyError> {
    // TODO: Implement sync command
    // 1. Scan source and destination
    // 2. Generate sync plan
    // 3. Execute plan
    todo!("Implement sync command")
}
